/**
 * The root screen: proof that the app builds and serves (AC-08). The real
 * layout lands with m0-08 and the root redirect with m0-14.
 */
export default function HomePage() {
  return (
    <main>
      <h1 data-testid="app-title">Vextrus</h1>
    </main>
  );
}
