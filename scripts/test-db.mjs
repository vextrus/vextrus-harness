#!/usr/bin/env node
/**
 * The database lane's entry point. No schema exists at m0-01, so there is
 * nothing to check yet; the increment that adds drizzle fills this in.
 */
console.log('test:db — no schema in the tree yet, nothing to check');
