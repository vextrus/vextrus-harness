#!/usr/bin/env node
/**
 * The database lane's entry point. The schema, its migrations and their tests
 * arrive in a later increment; the entry point exists now so every increment
 * after this one extends it instead of inventing it.
 */
process.stdout.write('ok test:db — no database lane at this milestone (0 suites)\n')
