node_modules
.next
.next-verify
# per-process scratch tsconfig: `next build` rewrites it so tsconfig.json is left alone
tsconfig.verify-*.json
next-env.d.ts
*.tsbuildinfo
.DS_Store
.env
.env.local

# e2e run artifacts: traces, screenshots, videos — scratch, never tree
var/e2e/

# The cad lane's Python environment and tool caches. `cad/uv.lock` is committed
# and is the reproducible part; the materialised venv is this machine's alone.
cad/.venv/
cad/**/__pycache__/
cad/.pytest_cache/
cad/.ruff_cache/
